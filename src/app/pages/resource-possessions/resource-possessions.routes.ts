import { Routes } from '@angular/router';
import { ResourcePossessionsComponent } from './resource-possessions.component';

const routes: Routes = [
  {
    path: '',
    component: ResourcePossessionsComponent,
  },
];

export default routes;
